'use strict';

describe('app.inicio module', function() {

    beforeEach(module('app.inicio'));

    describe('view2 controller', function() {

        it('should ....', inject(function($controller) {
            //spec body
            var view2Ctrl = $controller('View2Ctrl');
            expect(view2Ctrl).toBeDefined();
        }));

    });
});